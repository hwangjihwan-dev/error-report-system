# API/URL 설계서

## 1. 문서 개요

본 문서는 오류신고 관리 시스템의 1차 개발 범위에 대한 URL 설계를 정의한다.

1차 개발에서는 `Thymeleaf`를 사용하여 서버에서 HTML 화면을 렌더링하는 방식으로 구현한다.  
따라서 본 문서는 순수 REST API 명세라기보다, 화면 요청 URL과 폼 처리 URL을 함께 정리한 URL 설계서이다.

2차 개발에서 React로 전환할 경우, `/api` 경로를 사용하는 REST API 구조로 확장할 예정이다.

---

## 2. URL 설계 원칙

- 화면 조회는 `GET` 메서드를 사용한다.
- 데이터 등록 및 변경 처리는 `POST` 메서드를 사용한다.
- 일반 사용자 기능은 `/reports` 경로를 사용한다.
- 관리자 기능은 `/admin` 경로를 사용한다.
- 로그인하지 않은 사용자는 인증이 필요한 URL에 접근할 수 없다.
- 일반 사용자는 관리자 URL에 접근할 수 없다.

---

## 3. 회원 기능

| 기능 | Method | URL | 설명 | 권한 |
|---|---|---|---|---|
| 회원가입 화면 | GET | `/signup` | 회원가입 화면을 조회한다. | 비회원 |
| 회원가입 처리 | POST | `/signup` | 회원가입 요청을 처리한다. | 비회원 |
| 로그인 화면 | GET | `/login` | 로그인 화면을 조회한다. | 비회원 |
| 로그인 처리 | POST | `/login` | 로그인 요청을 처리한다. | 비회원 |
| 로그아웃 | POST | `/logout` | 로그아웃을 처리한다. | 로그인 사용자 |

### 3-1. 회원가입 화면

| 항목 | 내용 |
|---|---|
| Method | GET |
| URL | `/signup` |
| 설명 | 회원가입 화면을 반환한다. |
| View | `auth/signup` |
| 권한 | 비회원 |

### 3-2. 회원가입 처리

| 항목 | 내용 |
|---|---|
| Method | POST |
| URL | `/signup` |
| 설명 | 회원가입 요청을 처리한다. |
| 요청값 | `loginId`, `password`, `name` |
| 성공 시 | 로그인 화면으로 이동 |
| 실패 시 | 회원가입 화면으로 이동 후 오류 메시지 표시 |
| 권한 | 비회원 |

#### 요청값

| 파라미터 | 타입 | 필수 | 설명 |
|---|---|---|---|
| loginId | String | Y | 로그인 아이디 |
| password | String | Y | 비밀번호 |
| name | String | Y | 사용자 이름 |

---

### 3-3. 로그인 화면

| 항목 | 내용 |
|---|---|
| Method | GET |
| URL | `/login` |
| 설명 | 로그인 화면을 반환한다. |
| View | `auth/login` |
| 권한 | 비회원 |

### 3-4. 로그인 처리

| 항목 | 내용 |
|---|---|
| Method | POST |
| URL | `/login` |
| 설명 | 로그인 요청을 처리한다. |
| 요청값 | `loginId`, `password` |
| 성공 시 | 사용자 권한에 따라 메인 화면으로 이동 |
| 실패 시 | 로그인 화면으로 이동 후 오류 메시지 표시 |
| 권한 | 비회원 |

#### 요청값

| 파라미터 | 타입 | 필수 | 설명 |
|---|---|---|---|
| loginId | String | Y | 로그인 아이디 |
| password | String | Y | 비밀번호 |

---

### 3-5. 로그아웃

| 항목 | 내용 |
|---|---|
| Method | POST |
| URL | `/logout` |
| 설명 | 로그인 세션을 종료한다. |
| 성공 시 | 로그인 화면으로 이동 |
| 권한 | 로그인 사용자 |

---

## 4. 사용자 오류신고 기능

| 기능 | Method | URL | 설명 | 권한 |
|---|---|---|---|---|
| 내 오류신고 목록 | GET | `/reports` | 본인이 작성한 오류신고 목록을 조회한다. | 일반 사용자 |
| 오류신고 등록 화면 | GET | `/reports/new` | 오류신고 등록 화면을 조회한다. | 일반 사용자 |
| 오류신고 등록 처리 | POST | `/reports` | 오류신고 등록 요청을 처리한다. | 일반 사용자 |
| 오류신고 상세 | GET | `/reports/{id}` | 본인이 작성한 오류신고 상세를 조회한다. | 일반 사용자 |
| 오류신고 수정 화면 | GET | `/reports/{id}/edit` | 본인이 작성한 오류신고 수정 화면을 조회한다. | 일반 사용자 |
| 오류신고 수정 처리 | POST | `/reports/{id}/edit` | 본인이 작성한 오류신고 수정 요청을 처리한다. | 일반 사용자 |
| 오류신고 삭제 | POST | `/reports/{id}/delete` | 본인이 작성한 오류신고 삭제 요청을 처리한다. | 일반 사용자 |

### 4-1. 내 오류신고 목록

| 항목 | 내용 |
|---|---|
| Method | GET |
| URL | `/reports` |
| 설명 | 로그인한 사용자가 본인이 작성한 오류신고 목록을 조회한다. |
| View | `report/list` |
| 권한 | 일반 사용자 |

#### 요청 파라미터

| 파라미터    | 타입      | 필수 | 기본값 | 설명 |
|---------|---------|---|-----|---|
| page    | Integer | N | 0   | 페이지 번호 |
| keyword | String  | N | 10  | 페이지 크기 |

#### 조회 항목

| 항목 | 설명 |
|---|---|
| id | 오류신고 번호 |
| title | 제목 |
| status | 처리상태 |
| createdAt | 등록일 |
| answerYn | 답변 여부 |

---

### 4-2. 오류신고 등록 화면

| 항목 | 내용 |
|---|---|
| Method | GET |
| URL | `/reports/new` |
| 설명 | 오류신고 등록 화면을 반환한다. |
| View | `report/form` |
| 권한 | 일반 사용자 |

### 4-3. 오류신고 등록 처리

| 항목 | 내용 |
|---|---|
| Method | POST |
| URL | `/reports` |
| 설명 | 오류신고 등록 요청을 처리한다. |
| 요청값 | `title`, `content` |
| 성공 시 | 내 오류신고 목록 화면으로 이동 |
| 실패 시 | 오류신고 등록 화면으로 이동 후 오류 메시지 표시 |
| 권한 | 일반 사용자 |

#### 요청값

| 파라미터 | 타입 | 필수 | 설명 |
|---|---|---|---|
| title | String | Y | 오류신고 제목 |
| content | String | Y | 오류신고 내용 |

---

### 4-4. 오류신고 상세

| 항목 | 내용 |
|---|---|
| Method | GET |
| URL | `/reports/{id}` |
| 설명 | 본인이 작성한 오류신고 상세 내용을 조회한다. |
| View | `report/detail` |
| 권한 | 일반 사용자 |

#### Path Variable

| 변수명 | 타입 | 설명 |
|---|---|---|
| id | Long | 오류신고 ID |

#### 조회 항목

| 항목 | 설명 |
|---|---|
| id | 오류신고 번호 |
| title | 제목 |
| content | 내용 |
| status | 처리상태 |
| answer | 관리자 답변 |
| answeredAt | 답변일시 |
| createdAt | 등록일시 |

---

### 4-5. 오류신고 수정 화면

| 항목 | 내용 |
|---|---|
| Method | GET |
| URL | `/reports/{id}/edit` |
| 설명 | 본인이 작성한 오류신고 수정 화면을 반환한다. |
| View | `reports/edit` |
| 권한 | 일반 사용자 |

#### 처리 조건
- 본인이 작성한 오류신고만 수정 화면에 접근할 수 있다.
- 관리자 답변이 등록되지 않은 오류신고만 수정할 수 있다.

---

### 4-6. 오류신고 수정 처리

| 항목 | 내용 |
|---|---|
| Method | POST |
| URL | `/reports/{id}/edit` |
| 설명 | 본인이 작성한 오류신고의 제목과 내용을 수정한다. |
| 요청값 | `title`, `content` |
| 성공 시 | 오류신고 상세 화면으로 이동 |
| 실패 시 | 오류신고 수정 화면으로 이동 후 오류 메시지 표시 |
| 권한 | 일반 사용자 |

#### 요청값

| 파라미터 | 타입 | 필수 | 설명 |
|---|---|---|---|
| title | String | Y | 오류신고 제목 |
| content | String | Y | 오류신고 내용 |

#### 처리 조건
- 본인이 작성한 오류신고만 수정할 수 있다.
- 관리자 답변이 등록되지 않은 오류신고만 수정할 수 있다.

---

### 4-7. 오류신고 삭제

| 항목 | 내용 |
|---|---|
| Method | POST |
| URL | `/reports/{id}/delete` |
| 설명 | 본인이 작성한 오류신고를 삭제한다. |
| 성공 시 | 내 오류신고 목록 화면으로 이동 |
| 권한 | 일반 사용자 |

#### 처리 조건
- 본인이 작성한 오류신고만 삭제할 수 있다.
- 관리자 답변이 등록되지 않은 오류신고만 삭제할 수 있다.

---

## 5. 관리자 오류신고 기능

| 기능 | Method | URL | 설명 | 권한 |
|---|---|---|---|---|
| 전체 오류신고 목록 | GET | `/admin/reports` | 전체 오류신고 목록을 조회한다. | 관리자 |
| 오류신고 상세 | GET | `/admin/reports/{id}` | 오류신고 상세를 조회한다. | 관리자 |
| 관리자 답변 등록 | POST | `/admin/reports/{id}/answer` | 관리자 답변을 등록한다. | 관리자 |
| 관리자 답변 수정 | POST | `/admin/reports/{id}/answer/edit` | 관리자 답변을 수정한다. | 관리자 |

### 5-1. 전체 오류신고 목록

| 항목 | 내용 |
|---|---|
| Method | GET |
| URL | `/admin/reports` |
| 설명 | 관리자가 전체 오류신고 목록을 조회한다. |
| View | `admin/report-list` |
| 권한 | 관리자 |

#### 요청 파라미터

| 파라미터   | 타입      | 필수 | 기본값 | 설명      |
|--------|---------|---|-----|---------|
| page   | Integer | N | 0   | 페이지 번호  |
| status | String | N |  | 처리상태 필터 |
| keyword | String | N |  | 제목 또는 내용 검색어 |

#### 조회 항목

| 항목 | 설명 |
|---|---|
| id | 오류신고 번호 |
| title | 제목 |
| writerName | 작성자 이름 |
| status | 처리상태 |
| createdAt | 등록일 |
| answerYn | 답변 여부 |

---

### 5-2. 오류신고 상세

| 항목 | 내용 |
|---|---|
| Method | GET |
| URL | `/admin/reports/{id}` |
| 설명 | 관리자가 오류신고 상세 내용을 조회한다. |
| View | `admin/report-detail` |
| 권한 | 관리자 |

#### Path Variable

| 변수명 | 타입 | 설명 |
|---|---|---|
| id | Long | 오류신고 ID |

#### 조회 항목

| 항목 | 설명 |
|---|---|
| id | 오류신고 번호 |
| title | 제목 |
| content | 내용 |
| writerName | 작성자 이름 |
| status | 처리상태 |
| answer | 관리자 답변 |
| adminName | 답변한 관리자 이름 |
| answeredAt | 답변일시 |
| createdAt | 등록일시 |

---

### 5-3. 처리상태 변경

| 항목 | 내용 |
|---|---|
| Method | POST |
| URL | `/admin/reports/{id}/status` |
| 설명 | 관리자가 오류신고에 대한 답변을 등록하고 처리상태를 함께 변경한다. |
| 요청값 | `status` |
| 성공 시 | 관리자 오류신고 상세 화면으로 이동 |
| 실패 시 | 관리자 오류신고 상세 화면으로 이동 후 오류 메시지 표시 |
| 권한 | 관리자 |

#### Path Variable

| 변수명 | 타입 | 설명 |
|---|---|---|
| id | Long | 오류신고 ID |

#### 요청값

| 파라미터 | 타입 | 필수 | 설명 |
|---|---|---|---|
| answer | String | Y | 관리자 답변 내용 |
| status | String | Y | 처리상태 |

#### 허용 상태값

| 상태값 | 설명 |
|---|---|
| 접수 | 오류신고가 처음 등록된 상태 |
| 처리중 | 관리자가 오류 내용을 확인하고 처리 중인 상태 |
| 완료 | 답변 등록 또는 처리가 완료된 상태 |

#### 처리 내용
- 답변 내용을 저장한다.
- 선택한 처리상태를 저장한다.
- 답변한 관리자 ID를 저장한다.
- 답변일시를 저장한다.
- 수정일시를 갱신한다.

---

### 5-4. 관리자 답변 등록

| 항목 | 내용                              |
|---|---------------------------------|
| Method | POST                            |
| URL | `/admin/reports/{id}/answer`    |
| 설명 | 관리자가 오류신고에 대한 답변을 등록한다.         |
| 요청값 | `answer`, `status`              |
| 성공 시 | 관리자 오류신고 상세 화면으로 이동             |
| 실패 시 | 관리자 오류신고 상세 화면으로 이동 후 오류 메시지 표시 |
| 권한 | 관리자                             |

#### Path Variable

| 변수명 | 타입 | 설명 |
|---|---|---|
| id | Long | 오류신고 ID |

#### 요청값

| 파라미터 | 타입 | 필수 | 설명 |
|---|---|---|---|
| answer | String | Y | 관리자 답변 내용 |

#### 처리 내용

- 답변 내용을 저장한다.
- 답변한 관리자 ID를 저장한다.
- 답변일시를 저장한다.
- 처리상태를 `처리완료`로 변경한다.

---

### 5-5. 관리자 답변 수정

| 항목 | 내용                                |
|---|-----------------------------------|
| Method | POST                              |
| URL | `/admin/reports/{id}/answer/edit` |
| 설명 | 관리자가 기존 답변 내용과 처리상태를 수정한다. |
| 요청값 | `answer`, `status`                |
| 성공 시 | 관리자 오류신고 상세 화면으로 이동               |
| 실패 시 | 관리자 오류신고 상세 화면으로 이동 후 오류 메시지 표시   |
| 권한 | 관리자                               |

#### Path Variable

| 변수명 | 타입 | 설명 |
|---|---|---|
| id | Long | 오류신고 ID |

#### 요청값

| 파라미터 | 타입 | 필수 | 설명 |
|---|---|---|---|
| answer | String | Y | 관리자 답변 내용 |
| status | String | Y | 처리상태 |

#### 처리 내용
- 답변 내용을 수정한다.
- 선택한 처리상태를 저장한다.
- 답변한 관리자 ID를 저장한다.
- 답변일시를 갱신한다.
- 수정일시를 갱신한다.

---

## 6. 권한 정책

| URL 패턴 | 접근 권한 | 설명 |
|---|---|---|
| `/signup` | 비회원 | 회원가입 |
| `/login` | 비회원 | 로그인 |
| `/logout` | 로그인 사용자 | 로그아웃 |
| `/reports/**` | 일반 사용자 | 사용자 오류신고 기능 |
| `/admin/**` | 관리자 | 관리자 기능 |

---

## 7. 2차 REST API 전환 예정

1차 개발에서는 Thymeleaf 기반 화면 렌더링과 폼 전송 방식으로 구현한다.

2차 개발에서 React로 전환할 경우, 아래와 같이 REST API 구조로 변경하는 것을 고려한다.

| 기능 | Method | URL |
|---|---|---|
| 회원가입 | POST | `/api/auth/signup` |
| 로그인 | POST | `/api/auth/login` |
| 내 오류신고 목록 조회 | GET | `/api/reports` |
| 오류신고 등록 | POST | `/api/reports` |
| 오류신고 상세 조회 | GET | `/api/reports/{id}` |
| 전체 오류신고 목록 조회 | GET | `/api/admin/reports` |
| 처리상태 변경 | PATCH | `/api/admin/reports/{id}/status` |
| 관리자 답변 등록 | POST | `/api/admin/reports/{id}/answer` |
| 관리자 답변 수정 | PATCH | `/api/admin/reports/{id}/answer` |